package com.avinash.paypay.test.foundation.base.interfaces

/**
 * Core level interface extending [Concern]
 * This needs to be implemented by every object which provides some or the other capability of an application.
 */
interface Capability : Concern
